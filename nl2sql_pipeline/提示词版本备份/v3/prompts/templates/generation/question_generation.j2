{# nl2sql_pipeline/src/nl2sql_pipeline/prompts/templates/generation/enhanced_question_generation.j2 #}

## 任务
基于数据库分析结果和业务场景，生成一个自然语言查询问题。

## 当前配置

### 场景信息
- **主场景**: {{ main_scenario.name }} - {{ main_scenario.description }}
- **子场景**: {{ sub_scenario.name }}
- **关注点**: 
{% for area in sub_scenario.focus_areas %}
  - {{ area }}
{% endfor %}

### 复杂度要求
- **级别**: {{ complexity }} (Level {{ complexity_config.level }})
- **描述**: {{ complexity_config.description }}
- **约束**: 最多{{ complexity_config.constraints.max_tables | default('不限') }}表，{{ complexity_config.constraints.max_joins | default('不限') }}个JOIN
- **必须使用**: {{ complexity_config.sql_capabilities.required | join(', ') }}
- **禁止使用**: {{ complexity_config.sql_capabilities.forbidden | join(', ') }}

### 当前用例
- **类型**: {{ use_case.name }}
- **描述**: {{ use_case.description }}

## 数据库信息

### 1. 表结构（共{{ tables|length }}个表）
{% for table in tables[:20] %}
#### 表{{ loop.index }}: {{ table.name }}
- **描述**: {{ table.description | default('无描述') }}
- **记录数**: {{ table.row_count | default('未知') }}
- **列信息**:
{% for col in table.columns[:10] %}
  - {{ col.name }} ({{ col.type }}): {{ (col.comment or col.description or '无注释') | truncate(40) }} [{{ col.classification.category if col.classification else '未分类' }}]
{% endfor %}
{% if table.columns|length > 10 %}  - ...还有{{ table.columns|length - 10 }}列{% endif %}
{% endfor %}
{% if tables|length > 20 %}...还有{{ tables|length - 20 }}个表{% endif %}

### 2. 实体关系
{% if er_analysis %}
#### 物理关系（外键）
{% for rel in er_analysis.physical[:10] %}
- {{ rel.from }} → {{ rel.to }}
{% endfor %}

#### 逻辑关系
{% for rel in er_analysis.logical[:5] %}
- {{ rel.tables | join(', ') }} ({{ rel.pattern }})
{% endfor %}

#### 概念关系
{% for rel in er_analysis.conceptual[:5] %}
- {{ rel.entity1 }} ↔ {{ rel.entity2 }}: {{ rel.relationship }}
{% endfor %}
{% endif %}

### 3. {{ use_case.name }}的可用操作

#### 表级操作
{% for op in use_case.operations.table_level %}
{% if op is mapping %}{% for k,v in op.items() %}- {{ k }}: {{ v|join(', ') }}
{% endfor %}{% else %}- {{ op }}{% endif %}
{% endfor %}

#### 列级操作
{% for op in use_case.operations.column_level %}
{% if op is mapping %}{% for k,v in op.items() %}- {{ k }}: {{ v|join(', ') }}
{% endfor %}{% else %}- {{ op }}{% endif %}
{% endfor %}

#### 列类型操作建议
{% for col_type, suggestions in use_case.column_type_focus.items() %}
**{{ col_type }}类型列**:
{% for suggestion in suggestions[:2] %}
- {{ suggestion }}
{% endfor %}
{% endfor %}

### 4. {{ complexity }}级别操作示例
{% if complexity_config.operations %}
{% for category, details in complexity_config.operations.items() %}
**{{ category }}**:
{% if details is mapping %}
{% for subcategory, examples in details.items() %}
- {{ subcategory }}: {{ examples[0] if examples is sequence else examples }}
{% endfor %}
{% else %}
{% for example in details[:2] if details is sequence %}
- {{ example }}
{% endfor %}
{% endif %}
{% endfor %}
{% endif %}

{% if complexity_config.level >= 3 %}
## 业务规则说明
在生成复杂或专家级别的问题时，需要明确以下业务评判标准：

### 性能评判标准
- **最佳/最优**：
  - 合同执行：完成率最高、延期天数最少、按时交付率最高
  - 承制单位：合同完成率高、平均执行周期短、质量问题少
  - 财务效率：资金使用率高、支付及时率高、成本控制好
- **最差/最劣**：
  - 合同执行：完成率最低、延期天数最多、风险等级高
  - 承制单位：合同违约率高、质量问题频发、交付延期严重
  - 财务状况：资金积压严重、支付延期多、成本超支大

### 风险评判标准
- **高风险**：延期超过90天、金额超过5000万、涉及关键装备、承制单位有不良记录
- **中风险**：延期30-90天、金额1000-5000万、非关键装备
- **低风险**：按期执行、金额1000万以下、常规装备

### 重要性判定
- **关键合同**：涉及主战装备、金额巨大（>1亿）、战略供应商、影响重大项目
- **重要合同**：金额较大（5000万-1亿）、涉及重要子系统、优先级高
- **一般合同**：常规采购、金额适中、标准化产品

### 时间维度分析
- **同比**：与去年同期相比（如2024年Q1 vs 2023年Q1）
- **环比**：与上一个时期相比（如3月 vs 2月）
- **趋势**：至少包含3个时间点的变化分析

### 分组分析维度
- **按承制单位**：分析不同供应商的表现差异
- **按装备类型**：航空、地面、海上装备的特点分析
- **按地区分布**：不同省份、城市的执行情况
- **按时间周期**：年度、季度、月度的变化趋势

### 异常识别标准
- **金额异常**：超出历史平均值的2倍标准差
- **时间异常**：执行周期超出正常范围50%以上
- **数量异常**：订单量突增或突降超过30%
{% endif %}

## 业务规则生成指导

当问题中包含需要明确定义的业务概念时（如"最好的"、"延期的"、"高风险"等），请生成具体的业务规则说明。

### 常见需要定义的概念类型：
1. **排序评价类**（最好/最差/领先/落后）→ 说明基于哪个字段排序
2. **阈值判断类**（大额/高风险/重要）→ 说明具体数值阈值
3. **时间计算类**（延期/提前/过期）→ 说明如何计算时间差
4. **状态判断类**（活跃/异常/合格）→ 说明判断条件

### 生成示例：
- 如果提到"执行最差"：需定义 → 基于合同表.延期天数 DESC 排序，取前10
- 如果提到"高风险"：需定义 → 合同表.风险等级='高' OR (延期天数>60 AND 金额>5000万)
- 如果提到"延期严重"：需定义 → DATEDIFF(实际完成日期, 计划完成日期) > 90

### 要求：
- 每个业务概念都要对应到具体的表和列
- 给出可以直接转换为SQL的条件
- 如果一个问题包含多个概念，分别定义每个概念

### 多规则示例：
问题："找出2024年高风险且执行最差的大额合同"
应生成3个业务规则：
1. "高风险" → 类型:阈值判断类 → 定义:风险等级='高' OR (延期>60 AND 金额>5000万)
2. "执行最差" → 类型:排序评价类 → 定义:按延期天数降序排序取前10
3. "大额" → 类型:阈值判断类 → 定义:合同金额>1亿

## 输出要求

基于"{{ sub_scenario.name }}"场景，分析并生成问题。注意：一个表可能有多种用途（如既是主表又参与子查询），一个列也可能有多个操作（如既是GROUP BY字段又是ORDER BY字段）。

```json
{
  "table_analysis": {
    "tables_used": [
      {
        "table_name": "表名",
        "operations": [
          {
            "operation_type": "FROM/JOIN/SUBQUERY等",
            "operation_detail": "具体操作描述，如：作为主表查询、LEFT JOIN到主表、在WHERE子句的IN子查询中",
            "purpose": "该操作的目的"
          }
        ],
        "selection_reason": "选择该表的整体原因"
      }
    ]
  },
  
  "column_analysis": {
    "columns_used": [
      {
        "column_full_name": "表名.列名",
        "column_type": "数据类型",
        "column_category": "numeric/date/text/categorical",
        "operations": [
          {
            "operation_type": "SELECT/WHERE/GROUP BY/ORDER BY/AGG等",
            "operation_detail": "具体操作，如：SUM聚合、等值条件过滤、分组字段、降序排序",
            "purpose": "该操作的目的"
          }
        ],
        "selection_reason": "选择该列的整体原因"
      }
    ]
  },
  
  "generated_question": {
    "question_text": "生成的中文业务问题",
    "question_focus": "问题关注的核心业务点",
    "expected_output": "期望查询返回什么样的结果集",
    "value_proposition": "这个查询对{{ sub_scenario.name }}的业务价值"{% if complexity_config.level >= 3 %},
    "business_rules": [
      {
        "concept": "识别到的业务概念（如'执行最差'）",
        "type": "概念类型（排序评价类/阈值判断类/时间计算类/状态判断类）",
        "definition": "具体定义说明",
        "sql_mapping": {
          "tables": ["涉及的表"],
          "columns": ["涉及的列"],
          "condition": "SQL条件或计算方式"
        }
      }
    ]{% endif %}
  }
}
```

## 示例说明

### 表的多重操作示例：
- 一个表可以同时作为主表（FROM）和子查询表（SUBQUERY）
- 一个表可以被多次JOIN（如自连接）
- 一个表可以在不同的子查询中使用

### 列的多重操作示例：
- contracts.contract_id: 既是JOIN条件，又是GROUP BY字段
- contracts.amount: 既在SELECT中显示，又用于SUM聚合，还用于ORDER BY排序
- milestones.date: 既用于WHERE条件过滤，又用于日期计算

## 注意事项

1. **表操作类型**：
   - FROM: 作为主表
   - INNER/LEFT/RIGHT/FULL JOIN: 各种连接
   - SUBQUERY: 在WHERE/HAVING/FROM子句中的子查询
   - UNION: 集合操作

2. **列操作类型**：
   - SELECT: 结果集展示
   - WHERE: 条件过滤
   - GROUP BY: 分组
   - ORDER BY: 排序
   - 聚合函数: SUM/AVG/COUNT/MAX/MIN
   - 窗口函数: ROW_NUMBER/RANK等（{{ complexity }}级别允许时）
   - 计算: 算术运算、日期计算等

3. **生成原则**：
   - 问题要具体、可执行
   - 符合{{ complexity }}复杂度要求
   - 体现{{ use_case.name }}特征
   - 贴合{{ sub_scenario.name }}场景需求
   - 充分利用数据库的关系结构
{% if complexity_config.level >= 3 %}
   - **复杂/专家级特殊要求**：
     - 涉及"最优/最差"等评判时，必须明确具体的评判指标
     - 使用具体的数值阈值，而非模糊描述
     - 多指标综合评判时，说明权重或优先级
     - 时间对比分析需明确对比维度（同比/环比）
{% endif %}

请基于以上信息，生成一个符合要求的查询问题，并详细说明表和列的使用方式。

{% if complexity_config.level >= 3 %}
### 复杂问题的输出示例：
问题文本："查找2024年各地区延期严重且金额巨大的高风险合同承制单位排名"
```json
{
  "generated_question": {
    "question_text": "查找2024年各地区延期严重且金额巨大的高风险合同承制单位排名",
    "business_rules": [
      {
        "concept": "延期严重",
        "type": "时间计算类",
        "definition": "实际完成日期与计划完成日期相差超过90天",
        "sql_mapping": {
          "tables": ["合同表"],
          "columns": ["合同表.实际完成日期", "合同表.计划完成日期"],
          "condition": "DATEDIFF(实际完成日期, 计划完成日期) > 90"
        }
      },
      {
        "concept": "金额巨大",
        "type": "阈值判断类",
        "definition": "合同金额超过1亿元",
        "sql_mapping": {
          "tables": ["合同表"],
          "columns": ["合同表.合同金额"],
          "condition": "合同金额 > 100000000"
        }
      },
      {
        "concept": "高风险",
        "type": "阈值判断类",
        "definition": "风险等级为高或满足高风险条件",
        "sql_mapping": {
          "tables": ["合同表"],
          "columns": ["合同表.风险等级", "合同表.延期天数", "合同表.金额"],
          "condition": "风险等级 = '高' OR (延期天数 > 60 AND 金额 > 50000000)"
        }
      },
      {
        "concept": "承制单位排名",
        "type": "排序评价类",
        "definition": "按符合条件的合同数量降序排列",
        "sql_mapping": {
          "tables": ["合同表", "承制单位表"],
          "columns": ["承制单位表.单位名称", "COUNT(合同表.合同ID)"],
          "condition": "GROUP BY 承制单位 ORDER BY COUNT(*) DESC"
        }
      }
    ]
  }
}
```
{% endif %}