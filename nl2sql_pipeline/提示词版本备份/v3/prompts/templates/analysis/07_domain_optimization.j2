你是一位经验丰富的数据架构师，正在对数据库进行深度分析后的领域知识优化。

## 背景信息

### 初始领域理解：
{{ initial_domain }}

### 数据库结构统计：
- 总表数量: {{ total_tables }}
- 总字段数量: {{ total_columns }}
- 字段类型分布:
{% for type, count in field_categories.items() %}
  - {{ type }}: {{ count }}个（占比{{ '%.1f' | format(count / total_columns * 100) }}%）
{% endfor %}

### 核心业务表分析：
{% for table in table_summaries %}
#### {{ loop.index }}. {{ table.name }}
- 业务描述: {{ table.description }}
- 业务类型: {{ table.type }}
{% if table.key_columns %}
- 关键字段: {{ table.key_columns | join(', ') }}
{% endif %}
{% if table.sample_columns %}
- 主要列注释示例:
{% for col in table.sample_columns[:3] %}
  - {{ col.name }}: {{ col.description }}
{% endfor %}
{% endif %}
{% endfor %}

### 数据库整体特征分析：
{% if analysis_insights %}
- 实体表数量: {{ analysis_insights.entity_tables }}个
- 关系表数量: {{ analysis_insights.relation_tables }}个
- 配置表数量: {{ analysis_insights.config_tables }}个
- 日志表数量: {{ analysis_insights.log_tables }}个
{% endif %}

## 任务要求

基于以上分析结果，请生成一个优化后的数据库领域描述。

### 优化目标：
1. **准确性提升**：基于表结构和字段分析，纠正初始理解中的偏差
2. **完整性补充**：识别初始描述中遗漏的重要业务领域
3. **深度增强**：揭示数据间的业务关系和数据流向
4. **实用性改进**：突出对开发者最有价值的领域知识

### 输出要求：
- 长度：200-300字
- 结构：先总体概述，再分述核心业务模块，最后总结数据特点
- 语言：专业、准确、易懂
- 重点：业务逻辑 > 技术细节

请直接输出优化后的领域描述，不需要解释优化过程。