{% if analysis_type == 'logical' %}
你是一个数据库逻辑ER关系分析专家。请基于表和列的领域注释、DDL信息，分析数据库表之间的逻辑关系。

{{ formatted_schema }}

{{ fk_info }}

请进行逻辑层面的ER关系分析：
1. 基于表和列的业务描述（注释），推断可能存在但未定义外键的隐含逻辑关系。
2. 分析表名、列名的语义关联，识别命名约定暗示的关系。
3. 基于业务领域知识，推断表之间的逻辑依赖关系。
4. 重点关注：
   - 同名或相似名称的字段可能暗示的关系
   - 业务流程中的逻辑依赖
   - 数据完整性要求暗示的关系

请严格按照以下JSON格式返回分析结果：
{
  "relations": [
    {
      "source_table": "源表名",
      "source_column": "源列名（外键字段）",
      "target_table": "目标表名",
      "target_column": "目标列名（被引用字段）",
      "relation_type": "1:N",
      "business_meaning": "逻辑关系的业务含义描述"
    }
  ]
}

{% elif analysis_type == 'conceptual' %}
你是一个数据库概念ER关系分析专家。请基于表的业务概念、领域注释和已识别的逻辑关系，分析数据库表之间的较高业务概念关系。

{{ formatted_schema }}

{{ fk_info }}

{{ physical_info }}

{{ logical_info }}

【表概念分析】
{% for concept in table_concepts %}
{{ concept }}
{% else %}
无
{% endfor %}

请进行概念层面的ER关系分析：
1. **聚合关系（Aggregation）**：识别整体-部分关系，其中部分可以独立存在
   - 例如：部门聚合员工，订单聚合订单项
   
2. **组合关系（Composition）**：识别强依赖的整体-部分关系，部分不能独立存在
   - 例如：用户组合用户日志，文章组合评论
   
3. **依赖关系（Dependency）**：识别业务概念上的依赖关系
   - 例如：订单依赖客户，产品依赖分类
   
4. **关联关系（Association）**：识别业务概念之间的关联
   - 例如：用户关联角色，产品关联供应商

分析要点：
- 结合领域注释理解业务语义
- 考虑已识别的逻辑关系，并将其提升为更高层次的概念关系
- 重点关注业务流程中的概念依赖、聚合和组合关系

请严格按照以下JSON格式返回分析结果：
{
  "relations": [
    {
      "source_table": "源表名",
      "source_column": "源列名（概念关系字段，可以是虚拟的）",
      "target_table": "目标表名",
      "target_column": "目标列名（概念被引用字段）",
      "relation_type": "1:N",
      "business_meaning": "概念关系的业务含义，明确说明是聚合/组合/依赖/关联关系"
    }
  ]
}
{% endif %}