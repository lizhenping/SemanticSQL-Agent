{# 问题生成提示词模板 #}
你是一个数据分析专家，需要为数据库查询生成自然语言问题。

## 场景信息
- **主场景**: {{ scenario.name }}
- **子场景**: {{ sub_scenario.name }}
- **场景描述**: {{ scenario.description }}
- **关注领域**: 
{% for area in sub_scenario.focus_areas %}
  - {{ area }}
{% endfor %}

## 复杂度要求
- **复杂度级别**: {{ complexity.name }} (Level {{ complexity.level }})
- **复杂度描述**: {{ complexity.description }}

### SQL能力要求
- **必须包含**: {{ complexity.sql_capabilities.required | join(', ') }}
- **允许使用**: {{ complexity.sql_capabilities.allowed | join(', ') }}
- **禁止使用**: {{ complexity.sql_capabilities.forbidden | join(', ') }}

### 具体操作示例
{% for operation_type, operations in complexity.operations.items() %}
**{{ operation_type }}**:
{% if operations is mapping %}
  {% for sub_type, sub_operations in operations.items() %}
  - {{ sub_type }}:
    {% for op in sub_operations %}
    - {{ op }}
    {% endfor %}
  {% endfor %}
{% else %}
  {% for operation in operations %}
  - {{ operation }}
  {% endfor %}
{% endif %}
{% endfor %}

## 参考示例
根据场景和复杂度，以下是一些参考问题类型：
{% if scenario_examples %}
{% for example in scenario_examples %}
- {{ example }}
{% endfor %}
{% endif %}

## 生成要求
1. 生成 {{ count }} 个符合上述要求的自然语言问题
2. 问题应该：
   - 符合{{ scenario.name }}下{{ sub_scenario.name }}的业务场景
   - 复杂度符合{{ complexity.name }}级别的要求
   - 自然流畅，像真实用户会提出的问题
   - 包含具体的业务含义，而不是抽象的数据库操作
3. 问题的多样性：
   - 涵盖子场景的不同关注领域
   - 使用不同的提问方式
   - 包含不同的筛选条件和分析维度

## 输出格式
请按以下JSON格式输出：
```json
{
  "questions": [
    {
      "question": "自然语言问题",
      "complexity_features": ["使用的主要SQL特性"],
      "focus_area": "对应的关注领域"
    }
  ]
}
```