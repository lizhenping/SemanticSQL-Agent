你现在是一名顶尖的数据架构师和业务专家。正在对已生成的列描述，请首先反思，每条是否之前标注的有明显不符合业务领域的，按照之前针对列理解的领域知识检查之前标注明显不合理的，并结合领域进行矫正。

# 任务
数据库领域知识仅仅是初步的了解存在错误，仅供参考，不要过度依赖。
请首先反思，每条是否之前标注的有明显不符合业务领域的，按照之前针对列理解的领域知识检查之前标注明显不合理的，并结合领域进行矫正。
检查并矫正字段 "{{ table_name }}.{{ column_name }}" 的描述，确保其准确性和业务逻辑的一致性。

# 上下文信息
1.  **数据库领域知识**:
    {{ domain_description }}

2.  **表结构 (Table Schema)**:
    {{ table_schema_ddl }}

3.  **字段信息**:
    - 字段名: {{ column_name }}
    - 数据类型: {{ column_type }}
    - 是否可为空: {{ is_nullable }}
    - 样例数据: {{ column_examples }}

4.  **当前生成的描述**:
    "{{ current_description }}"

5.  **字段分类信息**:
    - 类别: {{ field_category }}
    - 维度/度量: {{ dim_or_meas }}

# 矫正要求
请从以下角度检查并矫正描述：

1. **业务逻辑一致性**: 描述是否与数据库领域知识和实际业务场景一致？
2. **命名准确性**: 基于字段名和样例数据，当前描述的命名是否准确？
3. **分类匹配性**: 描述是否与字段的分类信息(类别、维度/度量)匹配？
4. **样例数据符合性**: 描述是否与实际的样例数据内容相符？
5. **格式规范性**: 是否严格遵循"注释-领域内业务说明"的格式？
6. **业务人员可能会使用简写首字母，简写首字母含义也是重要参考。

# 输出要求
- 必须输出JSON格式的结果
- 注释必须是中文
- 字段注释不超过15个汉字，业务说明不超过30个汉字
- 格式严格遵循："注释-领域内业务说明"
- 如果原描述准确无误，保持原描述；如果需要矫正，输出矫正后的描述

请严格按以下JSON格式返回：
```json
{
  "needs_correction": true/false,
  "corrected_description": "矫正后的描述",
  "correction_reason": "矫正的具体原因(如果needs_correction为true)"
}
```

例如：
- 如果原描述是"用户编号-系统分配的唯一用户标识"，但实际是订单表中的用户ID，应矫正为"用户编号-关联订单的用户唯一标识"
- 如果原描述格式不规范，如"用户的唯一标识符"，应矫正为"用户编号-系统分配的用户唯一标识"