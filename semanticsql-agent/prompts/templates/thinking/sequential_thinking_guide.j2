# Sequential Thinking 工具使用指南

## 工具定位

sequential_thinking 是一个深度分析工具，用于复杂决策和问题诊断。它不是常规工具，而是在需要深入思考时使用的"元工具"。

## 使用时机

### 1. 任务开始时的整体规划
- 当你接到生成N个训练数据的任务时
- 用于制定整体执行策略
- 分析数据库特点，决定生成重点

### 2. 反思后发现复杂问题
- sql_reflection 发现问题但原因不明确
- 错误信息模糊或涉及多个可能原因
- 需要权衡多个修正方案

### 3. 多次修正仍未成功
- 同一个问题修正2-3次仍有错误
- 可能存在对数据库的根本性误解
- 需要重新审视整个生成策略

### 4. 复杂场景设计
- 需要设计涉及3个以上表的复杂查询
- 业务逻辑特别复杂的场景
- 需要考虑性能优化的查询

### 5. 理解偏差诊断
- 生成的SQL虽然语法正确但不符合业务逻辑
- 怀疑对某些表或字段的理解有误
- 需要决定是否重新运行分析工具

## 不需要使用的情况

### 1. 简单明确的错误
- 字段名拼写错误
- 表名错误
- 简单的语法错误

### 2. 直接的修正建议
- sql_reflection 已经明确指出问题和解决方案
- 错误信息清晰，修正方法明确

### 3. 常规生成流程
- 正常的场景选择、问题生成、SQL生成
- 没有遇到特殊困难的情况

## 使用示例

### 示例1：复杂错误诊断
```
情况：SQL执行失败，错误信息提示"子查询返回多行"
思考：这可能是对表关系理解错误，或者查询逻辑设计有误

Action: sequential_thinking
Action Input: {
  "problem_description": "子查询返回多行错误，需要分析是表关系理解错误还是查询逻辑问题",
  "context": {
    "error": "Subquery returns more than 1 row",
    "sql": "SELECT ... WHERE id = (SELECT user_id FROM orders ...)",
    "scenario": "查询用户最近订单"
  },
  "memory": <当前记忆>
}
```

### 示例2：策略制定
```
情况：需要生成100个训练样本，数据库有50+个表
思考：需要制定策略，决定重点关注哪些表和场景

Action: sequential_thinking
Action Input: {
  "problem_description": "制定100个训练样本的生成策略，数据库复杂度高",
  "context": {
    "table_count": 50,
    "core_tables": ["users", "orders", "products", ...],
    "target_count": 100
  },
  "memory": <数据库分析结果>
}
```

### 示例3：根本性理解偏差
```
情况：多次生成的SQL都无法正确处理订单状态
思考：可能对状态字段的值域理解有误

Action: sequential_thinking
Action Input: {
  "problem_description": "多次生成的订单状态查询都有误，需要分析是否理解偏差",
  "context": {
    "failed_attempts": 3,
    "common_issue": "状态值不匹配",
    "status_field": "order_status"
  },
  "memory": <包含field_classification的记忆>
}
```

## 输出利用

sequential_thinking 会返回：
- `analysis`: 问题分析
- `root_cause`: 根本原因
- `solution_strategy`: 解决策略
- `recommended_actions`: 具体行动建议

根据这些输出，你可以：
1. 明确问题所在
2. 选择合适的修正工具
3. 避免重复犯错
4. 优化后续生成策略

## 记住

- sequential_thinking 是"思考如何思考"的工具
- 不要过度使用，只在真正需要深入分析时使用
- 它的输出是指导性的，最终决策仍由你做出
- 善用这个工具可以显著提高生成效率和质量