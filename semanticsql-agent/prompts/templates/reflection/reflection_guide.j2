## 反思-修正机制指导

### 何时进行反思

每当你完成一个SQL的生成和执行后，都应该使用 **sql_reflection** 工具进行质量评估。这个工具会帮助你：

1. **评估执行结果**
   - SQL是否成功执行？
   - 返回的数据量是否合理？
   - 结果是否符合业务逻辑？

2. **检查语义匹配**
   - SQL是否准确实现了问题的意图？
   - 是否遗漏或多余了查询条件？
   - 连接条件是否正确？

3. **审视生成质量**
   - 问题描述是否清晰无歧义？
   - SQL是否符合最佳实践？
   - 是否充分利用了数据库知识？

### 如何决定是否需要修正

sql_reflection 会返回一个评估结果，包含：
- `needs_revision`: 是否需要修正（true/false）
- `issues`: 发现的问题列表
- `suggestions`: 改进建议
- `recommended_action`: 推荐的修正工具

### 修正策略

当 `needs_revision` 为 true 时：

1. **首先考虑问题的复杂度**
   - 简单问题（如拼写错误、字段名错误）：直接调用推荐的工具
   - 复杂问题（如逻辑错误、理解偏差）：先使用 sequential_thinking 深入分析

2. **使用 sequential_thinking 的时机**
   - 当错误原因不明确时
   - 当需要在多个可能的解决方案中选择时
   - 当问题涉及对数据库理解的偏差时
   - 当多次修正仍未解决问题时

3. **精准修正原则**
   - 只修正有问题的部分，不要重做所有步骤
   - 保持场景和操作不变（这些是预定义的）
   - 可以修正：问题生成、SQL生成、数据库理解

### 修正示例

#### 示例1：简单的字段名错误
```
Reflection结果: {
  "needs_revision": true,
  "issues": ["使用了不存在的字段名 'user_name'"],
  "recommended_action": {
    "tool": "sql_generation",
    "reason": "字段名应该是 'username'"
  }
}

决策: 直接调用 sql_generation 重新生成SQL
```

#### 示例2：复杂的业务逻辑错误
```
Reflection结果: {
  "needs_revision": true,
  "issues": ["查询逻辑不符合业务需求", "可能误解了表之间的关系"],
  "recommended_action": {
    "tool": "sql_generation",
    "reason": "需要重新理解业务逻辑"
  }
}

决策: 先使用 sequential_thinking 分析：
- 是否正确理解了业务需求？
- 表关系的理解是否准确？
- 是否需要重新分析某个表的含义？
```

### 记住

- 反思是为了确保质量，不是为了完美
- 适度修正，避免过度优化
- 每次修正后都要重新验证
- 保持对整体进度的把握