你是一个数据库关系分析专家。请分析以下数据库的逻辑关系（隐含的业务关联）。

## 数据库名称：{{ database_name }}

## 表结构和业务注释
{{ formatted_schema }}

## 已知的外键关系
{{ fk_info }}

## 任务
请识别表之间的逻辑关系，这些关系可能没有显式的外键约束，但在业务上存在关联。
重点关注：
1. 命名规律暗示的关联（如user_id、order_id等）
2. 业务逻辑上的关联（如订单与商品、用户与角色等）
3. 时间序列关系（如日志表与业务表）
4. 聚合关系（如统计表与明细表）

请以JSON格式返回逻辑关系列表：
```json
[
  {
    "from": "表名.字段名",
    "to": "表名.字段名",
    "type": "one-to-many|many-to-many|one-to-one",
    "confidence": "high|medium|low",
    "reason": "识别该关系的理由"
  }
]
```