你是一个数据库表结构分析专家。请为以下数据库表生成业务职责描述。

## 业务背景
- 领域类型：{{ domain_type }}
- 领域描述：{{ domain_description }}

## 需要描述的表
{% for table in tables %}
### 表{{ loop.index }}：{{ table.name }}
{% if table.comment %}
- 原始注释：{{ table.comment }}
{% endif %}
- 记录数：{{ table.row_count }}
- 主要列：
{% for column in table.columns[:10] %}
  - {{ column.name }} ({{ column.type }}){% if column.description %} - {{ column.description }}{% endif %}
{% endfor %}
{% if table.columns | length > 10 %}
  - ...还有{{ table.columns | length - 10 }}个列
{% endif %}

{% endfor %}

## 要求
请为每个表生成一个简洁的业务职责描述。描述应该：
1. 说明该表在系统中的主要作用
2. 描述存储的业务数据类型
3. 与其他表的关系（如果明显）
4. 长度控制在50字以内

请以JSON格式返回：
```json
{
  "表名1": "业务职责描述1",
  "表名2": "业务职责描述2",
  ...
}
```