## 字段分类任务

请对表 {{ table_name }} 的字段进行分类。

### 表结构
{{ table_structure }}

{% if sample_data %}
### 样本数据
{{ sample_data }}
{% endif %}

### 分类标准

1. **dimensions（维度）**
   - 用于分组、筛选的分类字段
   - 通常是文本、枚举或分类值
   - 例如：地区、产品类别、客户类型、状态

2. **measures（度量）**
   - 可以进行数学运算的数值字段
   - 通常用于计算总和、平均值等
   - 例如：金额、数量、分数、价格

3. **identifiers（标识符）**
   - 唯一标识记录的字段
   - 通常是主键或唯一键
   - 例如：ID、编号、代码

4. **timestamps（时间戳）**
   - 时间相关的字段
   - 包括日期、时间、日期时间
   - 例如：创建时间、更新时间、交易日期

5. **descriptions（描述）**
   - 文本描述性字段
   - 通常是较长的文本
   - 例如：备注、说明、评论、地址

### 输出格式

请为每个字段指定类型和简短说明：

字段名: 类型 - 说明

例如：
- user_id: identifiers - 用户唯一标识
- order_amount: measures - 订单金额
- order_date: timestamps - 订单日期
- status: dimensions - 订单状态
- remark: descriptions - 订单备注

### 开始分类

请分析以上表结构和数据，对所有字段进行分类：