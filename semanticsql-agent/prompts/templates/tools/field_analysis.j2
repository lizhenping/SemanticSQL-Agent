你是一个数据库字段分析专家，请分析以下字段的语义信息。

## 上下文信息
数据库名称：{{ database_name }}
表名：{{ table_name }}
{% if domain_type %}领域类型：{{ domain_type }}{% endif %}
{% if domain_description %}业务描述：{{ domain_description }}{% endif %}

## 字段信息
字段名：{{ field_name }}
数据类型：{{ data_type }}
样本值：{{ sample_values[:3] | join(', ') }}
熵值等级：{{ entropy_level }}
是否主键：{{ is_primary }}
是否外键：{{ is_foreign }}
是否可空：{{ is_nullable }}
{% if business_desc %}字段注释：{{ business_desc }}{% endif %}

## 分类要求
请确定该字段的语义类别：

**category**: 字段类别，必须是以下之一：
   - identifier: 标识符（如ID、编号、唯一标识等）
   - measure: 度量值（可计算的数值，如金额、数量、分数等）
   - dimension: 维度（分类、分组字段，如状态、类型、等级等）
   - datetime: 日期时间（时间戳、日期、时间等）
   - text: 文本（描述性文本、名称、地址等）
   - boolean: 布尔值（是否标志、开关状态等）
   - other: 其他（无法归类的字段）

## 分析指导
- 优先考虑字段名称的语义含义
- 结合数据类型和样本值进行判断
- 熵值等级可以辅助判断字段特征：
  - high: 高唯一性（通常是标识符）
  - medium: 中等唯一性（可能是维度或度量值）
  - low: 低唯一性（通常是维度或状态字段）

请严格按照以下JSON格式返回结果：
{
  "category": "上述类别之一",
  "category_desc": "类别的中文描述"
}

注意：
- category 必须严格从上述7个类别中选择一个
- category_desc 应该是对应类别的完整中文描述，例如：
  - identifier → "标识符字段（用于唯一标识记录的字段，如ID、编号、主键等）"
  - measure → "度量值字段（可用于计算和统计的数值字段，如金额、数量、分数、价格等）"
  - dimension → "维度字段（用于分类和分组的字段，如状态、类型、等级、类别等）"
  - datetime → "日期时间字段（记录时间信息的字段，如创建时间、更新时间、生效日期等）"
  - text → "文本字段（存储描述性文本信息的字段，如姓名、地址、备注、描述等）"
  - boolean → "布尔值字段（表示是否状态的字段，如是否删除、是否有效、开关状态等）"
  - other → "其他类型字段（无法明确归类到上述类别的特殊字段）"