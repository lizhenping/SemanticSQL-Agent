你是一个数据库概念ER关系分析专家。请基于数据库表结构和注释信息，分析表之间的业务概念关系。

【数据库表结构】
{{ formatted_schema }}

【外键信息】
{{ fk_info }}

【表概念注释】
{% for table_name, table_data in tables.items() %}
表 {{ table_name }}：{{ table_data.comment or '无注释' }}
{% for column in table_data.columns %}
  - {{ column.name }}：{{ column.comment or '无注释' }}
{% endfor %}

{% endfor %}

请进行概念层面的ER关系分析，识别表之间的业务概念关系：

1. **聚合关系（Aggregation）**：识别整体-部分关系，其中部分可以独立存在
   - 例如：部门聚合员工，订单聚合订单项
   
2. **组合关系（Composition）**：识别强依赖的整体-部分关系，部分不能独立存在
   - 例如：用户组合用户日志，文章组合评论
   
3. **依赖关系（Dependency）**：识别业务概念上的依赖关系
   - 例如：订单依赖客户，产品依赖分类
   
4. **关联关系（Association）**：识别业务概念之间的关联
   - 例如：用户关联角色，产品关联供应商

分析要点：
- 结合表和列的注释理解业务语义
- 基于表名和列名的语义关联识别隐含关系
- 重点关注业务流程中的概念依赖、聚合和组合关系
- 可以识别跨多个表的复杂业务概念关系

请为整个数据库的ER关系分析提供一个整体的业务名称和描述，然后严格按照以下JSON格式返回分析结果：

{
  "business_name": "业务领域名称（如：客户订单履约流程）",
  "business_description": "整个业务流程的描述（2-3句话）",
  "triplets": [
    {
      "source_table": "源表名",
      "source_column": "源列名（关系字段）",
      "relation_semantic": "关系语义标识（如：customer_relation, order_item_relation）",
      "target_table": "目标表名",
      "target_column": "目标列名（被引用字段）",
      "business_meaning": "关系的业务含义描述（明确说明是聚合/组合/依赖/关联关系）",
      "confidence": 0.9
    }
  ]
}