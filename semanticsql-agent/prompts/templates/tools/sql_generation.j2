## 任务
基于数据库结构信息，生成SQL查询来回答用户的问题。

## 数据库信息
{{ context }}

## 配置
- SQL方言：{{ dialect }}
- 必须使用的操作：{{ operations | join(', ') if operations else '无限制' }}

## 用户问题
{{ question }}

## 要求
1. SQL语法必须正确，符合{{ dialect }}方言规范
2. 只返回SQL语句，不要其他解释
3. 使用合适的表和字段
4. 必要时使用JOIN连接表
5. 需要聚合时使用GROUP BY
6. 需要排序时使用ORDER BY
7. 考虑查询性能，避免不必要的复杂度

## SQL查询