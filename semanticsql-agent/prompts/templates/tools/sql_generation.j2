基于以下数据库结构信息，生成SQL查询来回答用户的问题。

{{ context }}

SQL方言：{{ dialect }}

用户问题：{{ question }}

请生成SQL查询，要求：
1. SQL语法正确，符合{{ dialect }}方言
2. 只返回SQL语句，不要其他解释
3. 使用合适的表和字段
4. 如果需要聚合，使用GROUP BY
5. 如果需要排序，使用ORDER BY
6. 考虑性能，避免不必要的复杂度

SQL查询：