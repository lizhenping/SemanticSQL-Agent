基于{{ main_name }}场景的{{ sub_name }}任务，生成{{ complexity }}级别的问题。

## 场景描述
{{ main_description }}

## 任务焦点
{{ focus_areas | join(', ') }}

## SQL操作要求
必须使用以下操作: {{ operations | join(', ') }}

## 复杂度要求
{{ complexity_desc }}

请生成一个符合上述要求的自然语言问题。