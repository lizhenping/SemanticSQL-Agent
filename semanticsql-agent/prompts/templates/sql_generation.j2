## 任务：生成 SQL 查询

### 用户查询
{{ query }}

### 数据库结构信息
{{ schema }}

{% if domain %}
### 业务领域分析
{{ domain }}
{% endif %}

{% if fields %}
### 字段分类信息
{{ fields }}
{% endif %}

{% if relations %}
### 表关系信息
{{ relations }}
{% endif %}

### SQL 生成要求

1. **准确性**
   - 只使用存在的表和列
   - 正确理解用户的查询意图
   - 使用合适的 JOIN 条件

2. **性能**
   - 避免使用 SELECT *，只选择需要的列
   - 使用适当的 WHERE 条件过滤数据
   - 添加 LIMIT 限制返回行数（默认 10-100 行）

3. **可读性**
   - SQL 语句格式化良好
   - 使用有意义的别名
   - 添加必要的注释

4. **安全性**
   - 只生成 SELECT 查询
   - 避免子查询嵌套过深
   - 注意防止笛卡尔积

### 示例格式

```sql
-- 查询说明
SELECT 
    t1.column1 AS alias1,
    t1.column2 AS alias2,
    AGG_FUNC(t2.column3) AS alias3
FROM 
    table1 t1
    JOIN table2 t2 ON t1.id = t2.table1_id
WHERE 
    t1.condition = 'value'
GROUP BY 
    t1.column1, t1.column2
ORDER BY 
    alias3 DESC
LIMIT 10;
```

请生成满足用户需求的 SQL 查询：