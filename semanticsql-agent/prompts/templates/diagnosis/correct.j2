{# 论文 §III.E Correct 算子 #}
{# 基于 errors E + 证据 Φ 修正 (q, s, r) #}
你是一位 SQL 修正专家。下面是一个有错误的 (question, SQL) 组合，以及检测到的错误和可用的修正证据。
请基于证据修正 SQL（必要时也修正 question），输出修正后的结果。

## 原始数据
- **问题**: {{ question }}
- **SQL（有错误）**: {{ sql }}
- **推理关注点**: {{ rationale }}
- **SQL 方言**: {{ dialect }}

## 检测到的错误
{{ errors }}

## 可用修正证据（来自知识库 K）
{{ evidence }}

## 修正要求
1. **替换无效 schema 元素**：用证据里的合法列/关系替换 SQL 中的错误引用
2. **保持问题意图**：除非 question 本身有歧义，否则不改 question；若必须改，只改措辞不改意图
3. **只输出修正后的 SQL 和 question**，不要解释修正过程

## 输出格式（严格 JSON）
```json
{
  "sql": "修正后的完整 SQL 语句",
  "question": "修正后的自然语言问题（若未改动则原样返回）"
}
```
