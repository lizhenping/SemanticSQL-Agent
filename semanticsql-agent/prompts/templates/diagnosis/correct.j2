{# 论文 §III.E Correct 算子（App. taxonomy:correct 提示骨架的实现） #}
{# 硬约束：只做局部、诊断指向的编辑；绝不重新生成整条 SQL；问题 q 永不修改。 #}
你是一位面向 text-to-SQL 数据合成的语义修正专家。下面是一个含语义错误的 (question, SQL) 组合，
以及诊断出的违例和对应的修正证据。请对 SQL 做局部修正。

## 原始数据
- **问题 (q，固定，绝不修改)**: {{ question }}
- **SQL（s，待局部修正）**: {{ sql }}
- **推理关注点 (r)**: {{ rationale }}
- **SQL 方言**: {{ dialect }}

## 检测到的违例（每条含七类标签 + 位置 + 违反条件）
{{ errors }}

## 可用修正证据（来自知识库 K，按违例类别检索）
{{ evidence }}

## 修正要求（论文 §III.E Correct 语义）
1. **局部编辑**：每个编辑落在诊断指向的位置——意图错配修在实现该意图的子句上，
   条件错配修在 WHERE/HAVING，粒度错配修在聚合表达式和 GROUP BY，
   元素误用/域规则违反修在所涉的列、值或 JOIN 上
2. **以证据为条件**：替换无效元素时必须使用证据里给出的合法列/关系/规则
3. **绝不重新生成整条查询**：只改违例涉及的部分，其余保持原样
4. **问题 q 永不修改**：即使问题看起来有歧义，也只调整 SQL 去正确回答它
5. 保持 SQL 方言为 {{ dialect }}

## 输出格式（严格 JSON，只要 SQL，不要 question 字段）
```json
{
  "sql": "修正后的完整 SQL 语句",
  "edits": ["逐条描述所做的局部编辑"]
}
```
