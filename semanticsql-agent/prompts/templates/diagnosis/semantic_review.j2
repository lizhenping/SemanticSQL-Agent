{# 论文 §III.E Diagnose 的 LLM 语义审查部分 #}
{# 捕捉结构检查抓不到的 holistic semantic inconsistencies #}
你是一位严格的 SQL 语义审查专家。请审查下面的 (question, SQL, rationale) 三元组，
找出**结构检查无法捕获的语义不一致**（即：语法正确、列存在、JOIN 合法，但语义上答非所问）。

## 审查对象
- **问题**: {{ question }}
- **SQL**: {{ sql }}
- **推理关注点**: {{ rationale }}

{% if domain_rules %}
## 领域规则（K2，供参考）
{{ domain_rules }}
{% endif %}

## 审查要点（只报语义层问题，不报语法/列不存在类问题）
1. **答非所问**：SQL 查询的结果是否真的回答了 question 问的内容？
2. **聚合误用**：例如对标识符求 AVG、该 COUNT 的写了 SELECT、该 DISTINCT 的漏了
3. **条件遗漏**：question 里的关键限定词（如"最大"、"去年"、"每个"）在 SQL 里没体现
4. **JOIN 多余/缺失**：语义上不需要的 JOIN，或需要的关联没做
5. **排序/分组语义**：ORDER BY/GROUP BY 是否符合问题意图

## 输出格式（严格 JSON，不要其他内容）
```json
{
  "issues": [
    {
      "clause": "SELECT / WHERE / JOIN / GROUP BY / ORDER BY",
      "table": "相关表名（可选）",
      "column": "相关列名（可选）",
      "detail": "具体语义问题描述"
    }
  ]
}
```

如果没有问题，返回 `{"issues": []}`。只返回 JSON，不要解释。
