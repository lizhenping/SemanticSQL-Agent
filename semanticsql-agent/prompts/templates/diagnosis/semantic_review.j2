{# 论文 §III.E Diagnose 的 LLM 语义审查部分（App. taxonomy:diagnose 提示骨架的实现） #}
{# 按论文 Definition 1 的顺序检查：问题有效性 → 意图匹配 → 使用一致性；
   每个违例按论文七类分类（Table semantic-taxonomy）输出 e_j=(t_j,u_j,λ_j,v_j)。 #}
你是一位面向 text-to-SQL 数据合成的语义审查专家。请按顺序审查下面的 (question, SQL, rationale) 三元组，
找出结构检查（语法/列存在/JOIN 合法/可执行）无法捕获的语义错误。

## 审查对象
- **问题 (q)**: {{ question }}
- **SQL (s)**: {{ sql }}
- **推理关注点 (r)**: {{ rationale }}

{% if knowledge_context %}
## 知识库证据（来自 K，供核对元素的真实含义与域规则）
{{ knowledge_context }}
{% endif %}

## 检查顺序（严格按此顺序，逐项检查）
1. **问题有效性 (question validity)**：问题 q 所问的事情是该数据库领域支持的吗？
   q 里的概念与操作和所涉列的真实含义、域规则一致吗？
   （例如对标识符列求平均值 = 问题无效。问题无效时立即停止后续检查。）
2. **意图匹配 (intent match)**：s 是否忠实表达了 q 的意图——任务、条件、聚合、粒度、结果形式？
   - I-1 任务错配：s 执行的任务与 q 请求的任务不同
   - I-2 条件错配：q 表达的条件 [C] 在 s 中缺失或被改变
   - I-3 聚合/粒度错配：聚合方式或分组粒度与问题不符
   - I-4 排序/结果形式错配：排序方向或结果形式与问题不符
3. **使用一致性 (usage consistency)**：s 对每列、每个值、每个 JOIN 的使用是否符合数据库的领域知识？
   - II-1 字段角色误用：施加于列 [A] 的操作与其现实含义矛盾（如对标识符聚合）
   - II-2 JOIN 误用：JOIN [J] 表达了一种不存在的关系
   - III-1 域规则违反：值 [V] 或过滤条件违反域规则 [R]（如收入统计未排除退款）

## 输出格式（严格 JSON，不要其他内容）
每个违例一条，按论文格式输出 (class, artifact, location, condition)：
```json
{
  "issues": [
    {
      "class": "I-1|I-2|I-3|I-4|II-1|II-2|III-1",
      "artifact": "q 或 s 或 r",
      "clause": "SELECT / WHERE / JOIN / GROUP BY / ORDER BY",
      "table": "相关表名（无则留空）",
      "column": "相关列名（无则留空）",
      "detail": "具体语义问题描述",
      "condition": "被违反的条件（论文 v_j）"
    }
  ]
}
```

注意：
- artifact 是**受影响的产物**：问题本身无效时填 "q"；SQL 的意图/使用错误填 "s"；推理链错误填 "r"。
- 如果没有任何问题，返回 `{"issues": []}`。只返回 JSON，不要解释。
