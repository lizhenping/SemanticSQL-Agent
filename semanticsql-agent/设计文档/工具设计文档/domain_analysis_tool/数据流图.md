# Domain Analysis Tool 数据流图文档

## 概述

本文档通过详细的流程图和数据流图，可视化展示 Domain Analysis Tool 基于 LLM 的智能分析过程。涵盖从 Neo4j 数据读取到业务知识图谱构建的完整数据处理流水线。

## 总体数据流架构

### 高层级数据流概览

```mermaid
graph TB
    subgraph "输入层"
        A[Neo4j Database节点] --> B[Schema Extraction结果]
        B --> C[Table/Column结构数据]
    end
    
    subgraph "处理层"
        D[DDL格式化器] --> E[LLM分析引擎]
        E --> F[JSON结构解析器]
        F --> G[业务知识构建器]
    end
    
    subgraph "输出层"  
        H[Domain节点] --> I[BusinessProblem节点]
        H --> J[KeyEntity节点]
        H --> K[BusinessRule节点]
        I --> L[业务知识图谱]
        J --> L
        K --> L
    end
    
    C --> D
    G --> H
    
    style E fill:#f9d71c,stroke:#333,stroke-width:4px
    style L fill:#27ae60,stroke:#333,stroke-width:3px
```

## 详细执行流程图

### 主流程：_run() 方法执行流

```mermaid
flowchart TD
    A[开始执行] --> B{检查schema_extraction依赖}
    B -->|失败| C[抛出DependencyError]
    B -->|成功| D[从Neo4j查询数据库结构]
    
    D --> E[格式化为DDL字符串]
    E --> F[构建LLM提示词]
    F --> G[调用LLM服务]
    
    G --> H{LLM调用成功?}
    H -->|失败| I[使用降级分析方案]
    H -->|成功| J[解析JSON响应]
    
    I --> K[基于规则的分析]
    J --> L{JSON解析成功?}
    L -->|失败| K
    L -->|成功| M[构建DomainKnowledge对象]
    K --> M
    
    M --> N[存储到Neo4j知识图谱]
    N --> O[生成执行结果报告]
    O --> P[返回成功消息]
    
    C --> Q[返回错误消息]
    P --> R[结束]
    Q --> R
    
    style G fill:#3498db,stroke:#2c3e50,stroke-width:2px
    style N fill:#e74c3c,stroke:#2c3e50,stroke-width:2px
    style M fill:#f39c12,stroke:#2c3e50,stroke-width:2px
```

## 数据转换流程

### Schema到DDL转换流程

```mermaid
sequenceDiagram
    participant Tool as DomainAnalysisTool
    participant Neo4j as Neo4j Database
    participant Formatter as DDL Formatter
    participant LLM as LLM Service

    Tool->>Neo4j: 查询数据库结构
    Note over Neo4j: MATCH (d:Database)-[:HAS_TABLE]->(t:Table)
    Neo4j-->>Tool: 返回结构化数据

    Tool->>Formatter: 传入原始Schema数据
    Note over Formatter: {tables: [...], columns: [...]}
    
    loop 对每个表
        Formatter->>Formatter: 生成CREATE TABLE语句
        Formatter->>Formatter: 处理列定义和约束
        Formatter->>Formatter: 添加主键和外键
    end
    
    Formatter-->>Tool: 返回完整DDL字符串
    
    Tool->>LLM: 发送DDL+提示词
    Note over LLM: 结构化分析请求
    LLM-->>Tool: 返回JSON格式分析结果
```

### Neo4j数据查询详细流程

```mermaid
graph LR
    subgraph "数据查询阶段"
        A[Database节点] --> B[HAS_TABLE关系]
        B --> C[Table节点]
        C --> D[HAS_COLUMN关系] 
        D --> E[Column节点]
        C --> F[HAS_FOREIGN_KEY关系]
        F --> G[ForeignKey节点]
    end
    
    subgraph "数据提取"
        H[table_name]
        I[column_name]
        J[data_type]
        K[is_primary_key]
        L[sample_values]
        M[entropy_level]
    end
    
    E --> H
    E --> I
    E --> J
    E --> K
    E --> L
    E --> M
    
    subgraph "DDL组装"
        N[CREATE TABLE语句]
        O[列定义]
        P[约束定义]
        Q[关系定义]
    end
    
    H --> N
    I --> O
    J --> O
    K --> P
    G --> Q
```

## LLM分析处理流程

### 提示词构建和LLM调用流程

```mermaid
flowchart LR
    subgraph "提示词工程"
        A[DDL内容] --> B[Jinja2模板加载]
        B --> C[02_domain_analysis_structured.j2]
        C --> D[模板渲染]
        D --> E[结构化提示词]
    end
    
    subgraph "LLM服务调用"
        F[LLM请求配置] --> G[temperature: 0.1]
        F --> H[max_tokens: 4000]
        F --> I[timeout: 120s]
        
        E --> J[LLM API调用]
        G --> J
        H --> J  
        I --> J
        
        J --> K{调用成功?}
        K -->|Yes| L[JSON响应]
        K -->|No| M[重试机制]
        M --> N{达到最大重试?}
        N -->|No| J
        N -->|Yes| O[降级处理]
    end
    
    subgraph "响应处理"
        L --> P[清理响应格式]
        P --> Q[移除```json标记]
        Q --> R[JSON.parse()]
        R --> S[数据验证]
        S --> T[DomainKnowledge对象]
    end
    
    style J fill:#3498db,stroke:#2c3e50,stroke-width:3px
    style T fill:#27ae60,stroke:#2c3e50,stroke-width:3px
```

### LLM六维分析数据流

```mermaid
graph TD
    A[LLM输入：DDL+提示词] --> B[跨行业首席数据架构师角色]
    
    B --> C{六维分析}
    
    C --> D[domain_type<br/>业务领域类型识别]
    C --> E[business_problems<br/>业务问题分析]  
    C --> F[solution_approaches<br/>解决方案识别]
    C --> G[key_entities<br/>核心实体抽取]
    C --> H[business_rules<br/>业务规则推导]
    C --> I[special_fields<br/>特殊字段规则]
    
    D --> J[JSON结构化输出]
    E --> J
    F --> J
    G --> J
    H --> J
    I --> J
    
    J --> K[DomainKnowledge对象构建]
    
    style C fill:#f39c12,stroke:#2c3e50,stroke-width:3px
    style J fill:#9b59b6,stroke:#2c3e50,stroke-width:2px
```

## Neo4j知识图谱构建流程

### 业务知识节点创建流程

```mermaid
sequenceDiagram
    participant Tool as DomainAnalysisTool
    participant Parser as JSON Parser  
    participant Builder as Knowledge Builder
    participant Neo4j as Neo4j Database

    Tool->>Parser: LLM JSON响应
    Parser->>Parser: 解析六维数据
    Parser-->>Tool: DomainKnowledge对象
    
    Tool->>Builder: 领域知识数据
    
    Note over Builder: 开始图谱构建
    Builder->>Neo4j: CREATE Domain节点
    Builder->>Neo4j: CREATE BusinessProblem节点们
    Builder->>Neo4j: CREATE KeyEntity节点们  
    Builder->>Neo4j: CREATE BusinessRule节点们
    Builder->>Neo4j: CREATE SolutionApproach节点们
    Builder->>Neo4j: CREATE SpecialField节点们
    
    Note over Builder: 创建关系网络
    Builder->>Neo4j: CREATE 领域-问题关系
    Builder->>Neo4j: CREATE 领域-实体关系
    Builder->>Neo4j: CREATE 领域-规则关系
    Builder->>Neo4j: CREATE 数据库-领域关系
    
    Neo4j-->>Builder: 创建结果统计
    Builder-->>Tool: 图谱构建完成
```

### 知识图谱结构详图

```mermaid
graph TB
    subgraph "数据库层"
        DB[(Database节点)]
    end
    
    subgraph "领域层" 
        Domain[Domain节点<br/>domain_type<br/>confidence<br/>timestamp]
    end
    
    subgraph "业务知识层"
        BP1[BusinessProblem-1<br/>问题描述<br/>优先级]
        BP2[BusinessProblem-2<br/>问题描述<br/>优先级]
        BP3[BusinessProblem-3<br/>问题描述<br/>优先级]
        
        KE1[KeyEntity-1<br/>实体描述<br/>重要性]
        KE2[KeyEntity-2<br/>实体描述<br/>重要性]
        KE3[KeyEntity-3<br/>实体描述<br/>重要性]
        
        BR1[BusinessRule-1<br/>规则描述<br/>类型]
        BR2[BusinessRule-2<br/>规则描述<br/>类型]
        BR3[BusinessRule-3<br/>规则描述<br/>类型]
        
        SA1[SolutionApproach-1<br/>方案描述]
        SA2[SolutionApproach-2<br/>方案描述]
        
        SF1[SpecialField-1<br/>字段规则]
    end
    
    DB -->|BELONGS_TO_DOMAIN| Domain
    
    Domain -->|HAS_PROBLEM| BP1
    Domain -->|HAS_PROBLEM| BP2  
    Domain -->|HAS_PROBLEM| BP3
    
    Domain -->|CONTAINS_ENTITY| KE1
    Domain -->|CONTAINS_ENTITY| KE2
    Domain -->|CONTAINS_ENTITY| KE3
    
    Domain -->|FOLLOWS_RULE| BR1
    Domain -->|FOLLOWS_RULE| BR2
    Domain -->|FOLLOWS_RULE| BR3
    
    Domain -->|HAS_SOLUTION| SA1
    Domain -->|HAS_SOLUTION| SA2
    
    Domain -->|HAS_SPECIAL_FIELD| SF1
    
    style Domain fill:#f39c12,stroke:#2c3e50,stroke-width:3px
    style DB fill:#3498db,stroke:#2c3e50,stroke-width:2px
```

## 错误处理和降级流程

### 异常处理数据流

```mermaid
flowchart TD
    A[正常执行流程] --> B{检查各种异常}
    
    B -->|依赖检查失败| C[DependencyError]
    B -->|Neo4j连接失败| D[Neo4jError] 
    B -->|LLM服务异常| E[LLMError]
    B -->|JSON解析失败| F[JSONDecodeError]
    B -->|正常执行| G[成功路径]
    
    C --> H[返回依赖错误信息]
    D --> I[返回数据库错误信息]
    
    E --> J[启动降级分析]
    F --> J
    
    J --> K[基于规则的关键词匹配]
    K --> L[构建低置信度结果]
    L --> M[继续执行存储流程]
    
    G --> N[正常结果输出]
    M --> O[降级结果输出]
    
    H --> P[终止执行]
    I --> P
    N --> Q[执行完成]
    O --> Q
    
    style J fill:#e74c3c,stroke:#2c3e50,stroke-width:2px
    style G fill:#27ae60,stroke:#2c3e50,stroke-width:2px
```

### 降级分析数据流

```mermaid
graph LR
    subgraph "降级触发条件"
        A[LLM服务不可用]
        B[JSON解析失败]
        C[响应超时]
        D[API配额耗尽]
    end
    
    A --> E[降级分析器]
    B --> E
    C --> E  
    D --> E
    
    E --> F[关键词匹配分析]
    F --> G[业务领域词典]
    
    G --> H[表名匹配]
    G --> I[字段名匹配]
    
    H --> J[计算匹配得分]
    I --> J
    
    J --> K[确定主要领域]
    K --> L[推断核心实体]
    L --> M[生成基础业务规则]
    
    M --> N[构建DomainKnowledge]
    N --> O[设置低置信度标记]
    O --> P[继续正常流程]
    
    style E fill:#f39c12,stroke:#2c3e50,stroke-width:2px
    style N fill:#e67e22,stroke:#2c3e50,stroke-width:2px
```

## 数据缓存和性能优化流程

### 缓存策略数据流

```mermaid
sequenceDiagram
    participant Client as Client Request
    participant Tool as DomainAnalysisTool
    participant Cache as Redis Cache
    participant Neo4j as Neo4j Database
    participant LLM as LLM Service

    Client->>Tool: 启动分析请求
    Tool->>Cache: 检查缓存(database_name+schema_hash)
    
    alt 缓存命中
        Cache-->>Tool: 返回缓存结果
        Tool-->>Client: 直接返回结果
    else 缓存未命中
        Tool->>Neo4j: 查询数据库结构
        Neo4j-->>Tool: 返回Schema数据
        Tool->>LLM: 发起分析请求
        LLM-->>Tool: 返回分析结果
        Tool->>Neo4j: 存储知识图谱
        Tool->>Cache: 缓存结果(TTL=24h)
        Tool-->>Client: 返回分析结果
    end
```

### 批量处理优化流程

```mermaid
graph TD
    A[批量节点创建请求] --> B{检查批次大小}
    B -->|超过阈值| C[分批处理]
    B -->|正常范围| D[直接执行]
    
    C --> E[分组业务问题节点]
    C --> F[分组核心实体节点] 
    C --> G[分组业务规则节点]
    
    E --> H[批次1: CREATE BusinessProblems]
    F --> I[批次2: CREATE KeyEntities]
    G --> J[批次3: CREATE BusinessRules]
    
    H --> K[创建关系网络]
    I --> K
    J --> K
    D --> K
    
    K --> L[返回创建统计]
    
    style C fill:#e74c3c,stroke:#2c3e50,stroke-width:2px
    style K fill:#27ae60,stroke:#2c3e50,stroke-width:2px
```

## 监控和日志数据流

### 执行监控流程

```mermaid
timeline
    title Domain Analysis Tool 执行时间线
    
    section 初始化阶段
        0s    : 工具启动
              : 依赖检查
              : Neo4j连接验证
    
    section 数据获取阶段  
        2s    : Neo4j Schema查询开始
        4s    : DDL格式化完成
              : 统计信息收集
    
    section LLM分析阶段
        5s    : LLM请求发起
        45s   : LLM响应接收
        46s   : JSON解析完成
    
    section 知识图谱构建
        47s   : Domain节点创建
        48s   : BusinessProblem节点批量创建
        49s   : 关系网络构建
        50s   : 分析完成
```

### 性能指标监控

```mermaid
pie title 执行时间分布 (总计: ~50秒)
    "LLM分析" : 40
    "Neo4j查询" : 4
    "DDL格式化" : 2
    "图谱构建" : 3
    "其他处理" : 1
```

## 数据流总结

### 关键数据流节点

1. **输入数据流**: Neo4j Database → Table/Column结构 → DDL字符串
2. **分析数据流**: DDL + 提示词 → LLM服务 → JSON结构化响应  
3. **解析数据流**: JSON响应 → 数据验证 → DomainKnowledge对象
4. **存储数据流**: DomainKnowledge → Neo4j节点创建 → 关系网络构建
5. **输出数据流**: 图谱统计 → 执行报告 → 结果返回

### 性能优化点

- **缓存机制**: Redis缓存分析结果，避免重复LLM调用
- **批量操作**: Neo4j批量节点创建，减少网络往返  
- **并行处理**: 图谱节点创建和关系构建并行执行
- **降级策略**: LLM不可用时自动切换到规则分析
- **连接池**: Neo4j连接复用，减少连接开销

通过这些详细的数据流图，开发者可以清楚地理解 Domain Analysis Tool 的完整执行过程，为代码实现和系统优化提供重要参考。