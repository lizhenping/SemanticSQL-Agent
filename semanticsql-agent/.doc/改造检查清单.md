# SemanticSQL Agent 改造检查清单

## 〇、前置条件检查

### 环境准备
- [ ] 当前代码已通过 Git 备份
- [ ] 创建新的功能分支 `feature/langchain-integration`
- [ ] Python 版本 >= 3.8
- [ ] 数据库连接测试通过
- [ ] 工作目录干净（无未提交的更改）

### 依赖准备
- [ ] 创建新的虚拟环境（推荐）
- [ ] 记录当前依赖版本（pip freeze > requirements_old.txt）

## 一、文件操作清单

### 需要删除的文件
- [ ] `debug_model.py`
- [ ] `list_models.py`
- [ ] `main.py`
- [ ] `simple_test.py`
- [ ] `test_llm.py`
- [ ] `setup.py`
- [ ] `agent/simple_generation_agent.py`
- [ ] `agent/data_generation_agent.py`
- [ ] `agent/smart_sql_agent.py`
- [ ] `config/trae_config.py`
- [ ] `tools/base_tool.py`

### 需要移动的文件
- [ ] `agent/callbacks.py` → `utils/callbacks.py`

### 需要创建的文件
- [ ] `utils/memory.py`
- [ ] `tools/analysis_tools/column_meaning_tool.py`
- [ ] `tools/analysis_tools/table_meaning_tool.py`
- [ ] `agent/sql_agent.py`

## 二、代码修改清单

### Agent 模块
- [ ] 重写 `base_agent.py` 使用 LangChain
- [ ] 创建新的 `sql_agent.py` 合并功能

### Tools 模块（所有工具都需要）
- [ ] 改为继承 `langchain.tools.BaseTool`
- [ ] 参数统一改为 `memory: Dict[str, Any]`
- [ ] 使用 Pydantic 模型定义输入
- [ ] 更新错误处理使用自定义异常

具体工具清单：
- [ ] `schema_extraction_tool.py`
- [ ] `domain_analysis_tool.py`
- [ ] `field_classification_tool.py`
- [ ] `er_analysis_tool.py`
- [ ] `scenario_tool.py`
- [ ] `operation_selection_tool.py`
- [ ] `question_generation_tool.py`
- [ ] `sql_generation_tool.py`
- [ ] `sql_validation_tool.py`
- [ ] `sql_execution_tool.py`
- [ ] `sql_reflection_tool.py`
- [ ] `sequential_thinking_tool.py`

### Utils 模块
- [ ] 实现 `memory.py` 继承 LangChain BaseMemory
- [ ] 更新 `callbacks.py` 使用 LangChain BaseCallbackHandler
- [ ] 更新 `llm_client.py` 集成 LangChain

### Models 模块
- [ ] 添加 `TaskConfig` 模型
- [ ] 添加 `TrainingDataResult` 模型
- [ ] 添加 `ColumnMeaning` 模型
- [ ] 添加 `TableMeaning` 模型
- [ ] 添加 `ERRelation` 模型
- [ ] 修改 `QueryScenario.operations` → `suggested_operations`

### CLI 模块
- [ ] 删除 `query` 命令
- [ ] 更新 `generate` 命令使用新 Agent
- [ ] 更新异常处理

### Prompts 模块
- [ ] 创建 `templates/system/` 目录
- [ ] 创建 `templates/tools/` 目录
- [ ] 创建 `templates/analysis/` 目录
- [ ] 更新 `manager.py` 使用 Jinja2

### Config 模块
- [ ] 删除批量处理相关配置
- [ ] 删除并发相关配置
- [ ] 删除重试机制配置
- [ ] 只保留 MySQL 配置

## 三、依赖安装清单

```bash
pip install langchain==0.2.16
pip install langchain-openai==0.1.25
pip install langchain-community==0.2.16
pip install jinja2==3.1.2
```

### 验证安装
- [ ] `python -c "import langchain; print(langchain.__version__)"`
- [ ] `python -c "from langchain.tools import BaseTool"`
- [ ] `python -c "from langchain.memory import BaseMemory"`

## 四、异常处理统一

所有文件需要更新导入：
```python
from semanticsql_agent.models.exceptions import (
    ToolExecutionError,
    ToolParameterError,
    DatabaseConnectionError,
    LLMError,
    AgentExecutionError,
    # ... 其他需要的异常
)
```

## 五、测试点

### 单元测试
- [ ] 每个工具的独立测试
- [ ] Memory 模块测试
- [ ] Callback 模块测试

### 集成测试
- [ ] Agent 初始化测试
- [ ] 工具调用链测试
- [ ] 记忆传递测试

### 端到端测试
- [ ] CLI generate 命令测试
- [ ] 完整的训练数据生成流程

## 六、验收标准

1. **LangChain 集成**
   - [ ] 所有工具继承自 LangChain BaseTool
   - [ ] Agent 使用 AgentExecutor
   - [ ] Memory 使用 BaseMemory

2. **参数统一**
   - [ ] 所有工具接收 memory 参数
   - [ ] 参数使用 Pydantic 验证

3. **功能正确**
   - [ ] 只支持批量生成（无单次查询）
   - [ ] 反思机制能识别问题并建议工具

4. **代码质量**
   - [ ] 无冗余文件
   - [ ] 统一的异常处理
   - [ ] 清晰的模块结构

## 七、阶段验证点

### 第一阶段完成后
- [ ] 所有冗余文件已删除
- [ ] 依赖包安装成功
- [ ] 无导入错误

### 第二阶段完成后
- [ ] `from utils.memory import DatabaseAnalysisMemory` 成功
- [ ] `from utils.callbacks import TrajectoryCallbackHandler` 成功
- [ ] 两个新工具文件创建完成

### 第三阶段完成后
- [ ] 每个工具可独立导入
- [ ] 工具的 args_schema 定义正确
- [ ] 至少一个工具测试通过

### 第四阶段完成后
- [ ] Agent 可以初始化
- [ ] 工具列表正确加载
- [ ] 记忆系统正常工作

### 第五阶段完成后
- [ ] CLI `generate` 命令可执行
- [ ] 错误处理正常工作
- [ ] 生成流程可以启动

### 第六阶段完成后
- [ ] 所有数据模型可导入
- [ ] Pydantic 验证通过

### 第七阶段完成后
- [ ] 提示词模板加载成功
- [ ] Agent 使用正确的提示词

## 八、注意事项

1. 保留原有的业务逻辑，只改变框架和接口
2. 每步改动后运行相关测试
3. 及时更新 API 文档
4. 使用 Git 分支进行改造，便于回滚
5. 遇到问题时参考"改造方案完善建议.md"

## 九、回滚计划

如果改造失败：
- 第1-2阶段：`git checkout -- .` 回到原始状态
- 第3阶段：保留基础模块，逐个修复工具
- 第4-5阶段：创建简化版Agent进行调试
- 任何阶段：`git checkout main` 返回主分支