# 异常集成修改清单

根据 `models/exceptions.py` 的异常定义，需要在整个文档体系中进行以下修改：

## 1. 需要更新的文档

### 1.1 工具文档更新

#### BaseTool-API.md
- **当前**：使用 `ToolExecutionError`（正确）
- **修改**：确保导入路径正确
  ```python
  from semanticsql_agent.models.exceptions import ToolExecutionError
  ```

#### 所有工具文档（15个）
需要更新异常处理部分，确保：
1. 使用正确的异常类
2. 导入路径统一
3. 错误代码符合规范

### 1.2 具体修改项

#### tools模块/生成工具/SQLGenerationTool.md
- **问题**：使用了未定义的 `SQLGenerationError` 和 `InvalidSchemaError`
- **修改**：
  ```python
  # 修改前
  except SQLGenerationError as e:
  except InvalidSchemaError as e:
  
  # 修改后
  from semanticsql_agent.models.exceptions import ToolExecutionError
  except Exception as e:
      raise ToolExecutionError(
          tool_name="sql_generation",
          reason=str(e)
      )
  ```

#### tools模块/分析工具/SchemaExtractionTool.md
- **当前**：使用 `DatabaseConnectionError`（正确）
- **修改**：添加正确的导入
  ```python
  from semanticsql_agent.models.exceptions import (
      DatabaseConnectionError,
      SchemaExtractionError
  )
  ```

### 2. 需要添加异常处理的文档

#### 2.1 Agent模块

##### BaseAgent-API.md
添加异常处理说明：
```python
from semanticsql_agent.models.exceptions import (
    AgentExecutionError,
    MaxIterationsError
)

# 在 run 方法中
try:
    result = self.agent_executor.run(...)
except MaxIterationsError:
    # 处理迭代超限
except Exception as e:
    raise AgentExecutionError(
        step="agent_execution",
        reason=str(e)
    )
```

##### SQLAgent-API.md
添加批量生成时的异常处理：
```python
from semanticsql_agent.models.exceptions import (
    DatabaseError,
    LLMError,
    AgentError
)
```

#### 2.2 Utils模块

##### DatabaseManager-API.md
```python
from semanticsql_agent.models.exceptions import (
    DatabaseConnectionError,
    SQLExecutionError,
    SchemaExtractionError
)
```

##### LLMClient-API.md
```python
from semanticsql_agent.models.exceptions import (
    LLMConnectionError,
    LLMResponseError,
    LLMTimeoutError
)
```

##### MemoryManager-API.md
```python
from semanticsql_agent.models.exceptions import (
    DataValidationError
)
```

#### 2.3 Config模块

##### Settings-API.md
```python
from semanticsql_agent.models.exceptions import (
    MissingConfigError,
    InvalidConfigError
)
```

### 3. CLI文档更新

#### CLI-API.md
更新错误处理装饰器部分：
```python
from semanticsql_agent.models.exceptions import (
    DatabaseConnectionError,
    AgentExecutionError,
    SemanticSQLException
)

def handle_errors(func):
    """统一的错误处理装饰器"""
    @wraps(func)
    def wrapper(*args, **kwargs):
        try:
            return func(*args, **kwargs)
        except DatabaseConnectionError as e:
            click.echo(f"数据库连接失败: {e.message}", err=True)
            sys.exit(1)
        except SemanticSQLException as e:
            click.echo(f"错误 [{e.error_code}]: {e.message}", err=True)
            sys.exit(1)
        except Exception as e:
            click.echo(f"未预期的错误: {e}", err=True)
            sys.exit(1)
    return wrapper
```

### 4. 参数对齐矩阵更新

在"错误处理"部分添加：
```python
### 6.1 参数验证
每个工具都应验证输入参数的完整性：

```python
from semanticsql_agent.models.exceptions import ToolParameterError

def _run(self, **kwargs):
    # 验证必需参数
    if "memory" not in kwargs:
        raise ToolParameterError(
            tool_name=self.name,
            param_name="memory",
            reason="Missing required parameter"
        )
```

### 5. 流程验证文档更新

添加异常处理示例：
```python
# 在数据库分析阶段
try:
    schema_result = schema_tool.run({"database_name": "ecommerce"})
except SchemaExtractionError as e:
    logger.error(f"Schema extraction failed: {e.message}")
    # Agent 可能需要重试或采用备选方案
```

### 6. 设计规范更新

在 `semanticsql-agent_DESIGN_SPEC.md` 中添加错误处理策略章节（如果还没有）。

## 实施步骤

1. **第一步**：更新所有工具文档的异常导入和使用
2. **第二步**：更新 Agent 和 Utils 模块的异常处理
3. **第三步**：更新 CLI 的错误处理
4. **第四步**：添加缺失的异常处理说明
5. **第五步**：验证所有异常类都被正确使用

## 注意事项

1. 所有异常都从 `semanticsql_agent.models.exceptions` 导入
2. 使用具体的异常类，避免捕获通用 `Exception`
3. 保留原始异常信息：使用 `from e`
4. 提供有用的错误上下文
5. 遵循错误代码规范

---

完成这些修改后，整个文档体系将与异常定义完全一致。