基于当前的项目配置，执行命令是：

  python main.py smart-analyze "全面分析这个数据库系统" --config configs/config.yaml --verbose --save-result complete_analysis.json

  或者使用默认配置路径的简化命令：

  python main.py smart-analyze "全面分析这个数据库系统" --verbose --save-result complete_analysis.json

  其他可用命令：

  # 查看帮助
  python main.py --help

  # 测试数据库连接
  python main.py test --config configs/config.yaml

  # 查看数据库结构
  python main.py schema --config configs/config.yaml

  # 交互式查询
  python main.py interactive --config configs/config.yaml

  # 单次查询
  python main.py run "查询用户数量" --config configs/config.yaml

LLM 模型数据库配置是:
--model "Qwen3-14B"
--api-key "not-needed"
--base-url "http://192.168.200.216:9009/v1"
--host "192.168.200.216" --port "13306" --user "testuser"
--password "testpass" --database "testdb"
generate --count "20" --output "test_ddd.json"
## Core Architecture (trae_agent Style)

我的环境是
source activate alphasql